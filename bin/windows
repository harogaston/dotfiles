#!/bin/bash

if [[ $UID != 0 ]]; then
	echo "Please run this script with sudo:"
	echo "sudo $0 $*"
	exit 1
fi

win=$(grep 'Windows' /boot/grub/grub.cfg | cut -d"'" -f2)
grub-reboot "$win"
reboot

exit 0
